[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
hyperdiv="*"
# I'm not sure why, but when I put some of the packges in dev packages
# I can't find certain imports, so keeping it all in packages for now
# since I'm not creating a module
pre-commit="*"
ruff="*"
